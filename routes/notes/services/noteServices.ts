import OpenAI from "openai";
import { type Note } from "@db/schema";
import { z } from "zod";



export const getNote = async (noteID: number) => {

};

export const getAllNotes = async () => {

};

export default { getAllNotes, getNote};
